read.csv('HIV.csv') -> D

D$WTnt->E
#looks at only the Wild type nucleotide column and stores into E

data.frame("Wildtype"=E,"Possible"=TRUE) ->data
Wildtype<-length(E)

HIV_possible_cPg <- function(Wildtype) {
  
  if {Wildtype[x]=="c" & Wildtype[x+1]=="g",print="yes"}

 else print="no"
  
  if {Wildtype[x]=="c" & Wildtype[x+1]=="a", print="yes"}
 
else print="no"
  
  if {Wildtype[x]=="c" & Wildtype[x+1]=="a", print="yes"}

return(print="no")
}

function(3)
# not complete 
  